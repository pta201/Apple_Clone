@model IEnumerable<Apple_Clone_Website.Models.ProductColor>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Color)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Color)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductColorID })
        </td>
    </tr>
}

</table>
